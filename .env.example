# Copy to .env for local development. Never commit the result — .env is
# gitignored, and this file exists so the shape is documented without the
# values.

# --- Database ----------------------------------------------------------------
# The app's own connection. MUST be a non-superuser, non-BYPASSRLS role: a
# superuser silently bypasses every row-level security policy, which would
# leave tenant isolation resting on the application layer alone.
DATABASE_URL=postgresql://os_app:password@localhost:5432/os
# Used only by Alembic for DDL. May be the table owner or a superuser; the
# running app must never use it.
ADMIN_DATABASE_URL=postgresql://postgres:password@localhost:5432/os

# Which database the test suite runs against. Unset, it derives one by
# suffixing ADMIN_DATABASE_URL's database name with "_test", so the dev
# database is never touched either way. Set it when the test database lives
# somewhere else, or is named something else — CI does.
TEST_DATABASE_URL=

# --- First tenant (deployment only) -------------------------------------------
# Read by scripts/bootstrap_tenant.py, not by the app, and only once: a
# production database is deliberately empty, and /auth/register cannot
# bootstrap because it needs an existing tenant and only ever hands out the
# clerk role. The script refuses to run against a database that already has
# users — a script that quietly adds administrators to a live system is a
# backdoor, not a convenience.
#
#   alembic upgrade head && python -m scripts.bootstrap_tenant
#
# Leave these empty for local development; migration 002 seeds a demo tenant
# when SEED_DEMO_DATA=true instead.
BOOTSTRAP_TENANT_NAME=
# Optional — derived from the tenant name when unset.
BOOTSTRAP_TENANT_SLUG=
BOOTSTRAP_ADMIN_EMAIL=
# Optional — "Administrator" when unset.
BOOTSTRAP_ADMIN_NAME=
# At least 12 characters — the script refuses shorter, because this account
# can approve and release payments. Set it in the shell for the one command
# that needs it rather than writing it to a file, and change it at first login.
BOOTSTRAP_ADMIN_PASSWORD=

# --- Security ----------------------------------------------------------------
# Every access token is signed with this. Generate one per environment:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
# The app refuses to start with the repository's placeholder unless DEBUG is on.
SECRET_KEY=generate-me
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Whether strangers may sign themselves into a tenant by naming its slug. Off:
# even at the default clerk role a self-registered user can create vendors,
# raise invoices, prepare payment runs and import bank statements.
ALLOW_SELF_REGISTRATION=false

# --- Environment -------------------------------------------------------------
# true only on a developer machine. When on, the 500 handler includes the
# exception type and message, and the startup checks on SECRET_KEY and
# CORS_ORIGINS are skipped.
DEBUG=true

# Comma-separated, or a JSON array. Must be the frontend's real origin in any
# deployed environment — the app refuses to start if it still says localhost
# while DEBUG is off.
CORS_ORIGINS=http://localhost:9002,http://127.0.0.1:9002

# --- AI (optional) -----------------------------------------------------------
# Nothing here is required to run the app; the AI features degrade to their
# rule-based fallbacks without keys.
AI_PROVIDER=claude
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_AI_API_KEY=

# --- OCR (optional) ----------------------------------------------------------
OCR_PROVIDER=ocr_space
OCR_SPACE_API_KEY=
# Google Document AI, if used instead
GOOGLE_CLOUD_PROJECT_ID=
GOOGLE_DOCUMENT_AI_PROCESSOR_ID=
GOOGLE_APPLICATION_CREDENTIALS=

# --- Notification email ------------------------------------------------------
# Notifications (approvals, SLA escalations, watchlist alerts) are queued in the
# same transaction as the action that produced them and delivered afterwards by
# a scheduler. Two things are needed in production, and mail is silent without
# BOTH of them:
#
#   1. SMTP_ENABLED=true and the SMTP_* settings below.
#      While this is false, messages are HELD untouched rather than attempted,
#      so switching it on later sends the backlog rather than finding it
#      expired. Nothing is lost by leaving it off; nothing is delivered either.
#
#   2. A scheduler running the drain, every minute is fine and it is cheap when
#      the queue is empty:
#
#          python -m scripts.dispatch_notifications
#
#      Without it, messages queue forever and nobody is ever told anything.
#      Render blueprint: see the "sarmaya-notifications" cron service.
#
# Check it is working after a deploy:
#   GET /api/v1/notifications/queue/summary   (needs workflow.manage)
SMTP_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@example.com
# Seconds. Kept short because a slow mail server should not stall the drain.
SMTP_TIMEOUT=5


# --- Workflow timers ---------------------------------------------------------
# Two jobs that only happen if something runs them:
#
#   * reminders   — nudge whoever can act on an item that is waiting but not
#                   yet late, so the escalation never has to fire
#   * escalations — when an SLA is breached, tell the escalation role once
#
#       python -m scripts.run_workflow_timers      (hourly is plenty)
#
# Escalation previously fired only when an admin clicked "Escalate overdue" in
# the Decision Inbox. Render blueprint: the "sarmaya-workflow-timers" service.
#
# How long an item may sit before a nudge, and the minimum gap between nudges:
REMINDER_INTERVAL_HOURS=24
# How far into the item's own SLA the nudge fires, as a fraction of it. A fixed
# delay cannot work — it has to be shorter than the deadline it protects, and
# the deadlines differ per state.
REMINDER_AT_SLA_FRACTION=0.5


# --- AI routing --------------------------------------------------------------
# Which models run which kind of task, cheapest first. Format:
#   provider:model, provider:model
# The second entry is the fallback, tried only when the first fails schema
# validation or comes back under-confident — so the stronger model costs
# nothing on the common path.
#
# Leave empty to use AI_PROVIDER for everything, which is how the system
# behaved before routing existed.
#
#   AI_ROUTE_EXTRACTION=gemini:gemini-2.5-flash, claude:claude-opus-4-8
#   AI_ROUTE_CLASSIFICATION=gemini:gemini-2.5-flash, claude:claude-opus-4-8
#   AI_ROUTE_REASONING=claude:claude-opus-4-8
AI_ROUTE_EXTRACTION=
AI_ROUTE_CLASSIFICATION=
AI_ROUTE_REASONING=

# Below this confidence a response is treated as a failure and the next
# candidate is tried.
AI_MIN_CONFIDENCE=0.4

# --- Integration Hub -------------------------------------------------------
# Sarmaya's own QuickBooks app credentials, not a client's — the same shape as
# the AI keys above being Sarmaya's rather than each tenant's. Per-tenant state
# (tokens, company id, posting accounts) lives in integration_connections and
# is never configured here.
#
# Leave these empty and the Integration Hub is simply unavailable: nothing
# connects, and a released payment queues nothing. That is the intended state
# for a deployment whose client has no accounting system to connect.
#
# Register the app at https://developer.intuit.com to obtain a client id and
# secret. QBO_REDIRECT_URI must match the redirect URI registered there
# exactly, and points at this API's callback route, not the frontend:
#   https://your-api-host/api/v1/integrations/quickbooks/callback
QBO_CLIENT_ID=
QBO_CLIENT_SECRET=
QBO_ENVIRONMENT=sandbox
QBO_REDIRECT_URI=

# Where the OAuth callback sends the browser once the handshake finishes. The
# callback answers with a redirect rather than JSON because its caller is a
# browser mid-navigation from Intuit's consent screen, which cannot read a
# response body.
FRONTEND_URL=http://localhost:9002

# --- Neon (written by `neon env pull`) ----------------------------------------
# `neon link` / `neon checkout` write NEON_BRANCH, DATABASE_URL and
# DATABASE_URL_UNPOOLED into .env for you. Declared in config.py because the
# settings model forbids unknown keys.
#
# READ THIS BEFORE TRUSTING WHAT NEON WROTE. Neon's DATABASE_URL is the *owner*
# role (neondb_owner). This app must not run as an owner: tenant isolation
# rests on row-level security, and an owner is not subject to it. After every
# `env pull`, repoint DATABASE_URL at the least-privilege role:
#
#   -- as the owner, AFTER the first migration so the tables exist:
#   CREATE ROLE os_app LOGIN PASSWORD '<generate one>';
#   GRANT CONNECT ON DATABASE neondb TO os_app;
#   GRANT USAGE ON SCHEMA public TO os_app;
#   GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO os_app;
#   GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO os_app;
#   ALTER DEFAULT PRIVILEGES IN SCHEMA public
#     GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO os_app;
#
# Create it with SQL, NOT `neon roles create`: roles made through Neon's API are
# granted neon_superuser, which carries BYPASSRLS — the one property this app's
# isolation cannot tolerate. `scripts/verify_deployment.py` fails the deploy if
# the app connects as a role that can bypass RLS; run it after any change here.
#
# Two endpoints per branch. Pooled (`-pooler`, PgBouncer transaction mode) for
# the app — safe here because set_tenant_context sets the GUC transaction-local
# and re-applies it per transaction. Unpooled (direct) for Alembic, which runs
# DDL: that is what ADMIN_DATABASE_URL should point at, as the owner.
NEON_BRANCH=
DATABASE_URL_UNPOOLED=

# The least-privilege role against the TEST database, for the RLS tests — the
# ones that connect as a role which cannot bypass RLS, or they pass while
# proving nothing. Derived from DATABASE_URL when unset, which is right only
# while the app database and the test database share a server. Point
# DATABASE_URL at a hosted branch and they no longer do, so state it here.
TEST_APP_DATABASE_URL=

# --- File storage -------------------------------------------------------------
# Where uploaded invoices and bank statements are written.
#
#   local  writes to UPLOAD_DIR. The default, so `docker compose up` needs
#          nothing configured. Correct on a developer machine.
#   s3     any S3-compatible bucket — Neon Object Storage, Cloudflare R2,
#          MinIO, or S3 itself.
#
# A DEPLOYMENT SHOULD USE s3. Hosted platforms give an ephemeral filesystem, so
# a local upload survives until the next restart or redeploy and then does not.
# That is worse here than ordinary data loss: an evidence pack records the
# SHA-256 of every attachment it references, so files vanishing turns a sealed
# audit document into hashes pointing at nothing. The pack still verifies its
# own seal and is still useless.
#
# Switching this does NOT strand existing files. Each file row records the
# backend that wrote it, and reads and deletes dispatch on that rather than on
# this setting — so files already on disk stay readable after the switch.
STORAGE_BACKEND=local
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=10

# Required when STORAGE_BACKEND=s3.
STORAGE_BUCKET=
# Leave empty for real AWS S3. Neon Object Storage, R2 and MinIO each give you
# their own endpoint. Credentials come from AWS_ACCESS_KEY_ID /
# AWS_SECRET_ACCESS_KEY below, which the bucket provider issues.
STORAGE_ENDPOINT_URL=
# Falls back to AWS_REGION when unset.
STORAGE_REGION=
