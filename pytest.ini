[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests that require a live Postgres database (auto-skipped if unavailable)
    performance: smoke tests that guard the no-caching decision. Deselect with -m 'not performance'.
