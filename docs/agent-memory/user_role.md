---
name: user-role
description: "Who the user is — the developer building Sarmaya OS on Windows, working to a spec owned by Andrew"
metadata: 
  node_type: memory
  type: user
  originSessionId: 83618a84-98c1-432e-a373-0e6e64f6a52e
---

The user (git author "bilal", email bilzz14k@gmail.com) is the **developer** building Sarmaya OS. The product **owner / spec author is Andrew** (see [[project-build-book]]).

- Works on **Windows** (PowerShell shell, project at `C:\python\sarmaya`, venv at `.\env`).
- Building to detailed written specs (MVP spec + Build Book). Andrew expects spec-adherence over ad-hoc interpretation — when in doubt, surface ambiguity rather than guessing.
- A separate **frontend already exists** and is integrated with the current backend (user can share it on request).

**How to apply:** Treat written specs as authoritative. When a request is ambiguous vs the spec, ask or propose a default explicitly rather than improvising. Frame work against the documented scope and non-negotiables.
