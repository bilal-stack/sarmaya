# NOTE: version ranges widened to allow Python 3.14-compatible wheels.
# Lower bounds are the first releases that ship cp314 wheels; upper bounds
# cap the major version to avoid surprise breaking changes.

# FastAPI / ASGI
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
python-multipart>=0.0.18

# Database
sqlalchemy>=2.0.43,<2.1
psycopg2-binary>=2.9.10,<3
alembic>=1.14,<2

# Authentication & security
python-jose[cryptography]>=3.3,<4
passlib[bcrypt]>=1.7.4,<2

# AI / OCR
google-cloud-vision>=3.8,<4
google-cloud-documentai>=3.1,<4  # Document AI
openai>=1.50
anthropic>=0.40  # Claude provider

# Utilities / Settings
python-dotenv>=1.0
pydantic>=2.11,<3
pydantic-settings>=2.6,<3
email-validator>=2.2,<3

# File handling
Pillow>=11.3,<12
PyPDF2==3.0.1

# HTTP client
httpx>=0.28,<1.0

# Email (async SMTP for notifications)
aiosmtplib>=3.0,<4

# OCR providers
requests>=2.32  # For OCR.space API
boto3>=1.35     # For AWS Textract (future)

# Dev / Testing (optional—remove from production)
pytest>=8.3,<9
pytest-asyncio>=0.24,<0.25
black>=24.10
ruff>=0.8
